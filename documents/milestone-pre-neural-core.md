# Milestone — Pre Neural Core (recover here)

| Field | Value |
|---|---|
| Date | 2026-08-02 |
| Label | `milestone/pre-neural-core-v0` |
| Intent | Recovery point before changing VIGIL’s base thinking: decorative Energy Core → **living labor-market world model** |
| Git | Local tag on the commit that lands this note; **do not force-push**; restore with `git checkout milestone/pre-neural-core-v0` or `git switch -c recover/pre-neural-core milestone/pre-neural-core-v0` |

## What was true at this milestone

- VIGIL ops shell: R3F + MediaPipe, floating panels, module rail, glass compare charts
- EnergyCore = visual orb (particles / rings), **not** data-embedded
- Orbit nodes = module shortcuts only
- Tower insights, Hiring Signals, Watchlist, Cities, sector/city chips — panel-first intelligence
- Collector: Celery + Ollama relevance; Plan B keyword only under critical heat
- Push sovereignty: commit locally freely; push only with Ashok double-YES; prefer one end-of-day push

## Why we froze here

Ashok ordered a recoverable baseline before we change the **base of the application and our thinking**: the core becomes an Obsidian-like neural graph of sectors ↔ cities ↔ companies ↔ roles, growing as we collect and summarise data (Tracks → Maps → Predicts made visible).

## How to recover

```bash
cd /home/user/Documents
git status
git tag -l 'milestone/*'
git checkout milestone/pre-neural-core-v0
# or branch:
git switch -c recover/pre-neural-core milestone/pre-neural-core-v0
```

Do **not** `reset --hard` unless Ashok explicitly orders it.
